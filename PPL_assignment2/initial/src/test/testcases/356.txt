string[] test_complex_LESS_operator(){
            boolean arr[3];
            arr[0]=true; arr[1]=true; arr[2]=false;
            if(arr[0]<arr[1] && arr[2]< TTrue || arr[1]<FFalse && ____<_567)
                do{
                    foo(abc);
                    g00_abc(____a);
                    
                    if(True<False)
                        break;
                    else
                        continue;
                }
                while(i<arr[1]+arr[2]+arr[0]);
            else
                return "Test LESS";
        }